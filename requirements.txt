# API interna de Aternos (login, estado, start/stop).
# Si Aternos cambia su web y el login falla, actualiza esta versión:
#   pip install --upgrade python-aternos
python-aternos>=3.0.6

# Carga de variables desde el archivo .env
python-dotenv>=1.0.0

# NOTA: la web (panel + /health para UptimeRobot) la sirve ahora el bot de Node
# (afk_bot.js con Express), así que Flask ya no es necesario.
